/** @type {import('tailwindcss').Config} */
const config = {
  content: [
    "./app/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}"
  ],
  theme: {
    extend: {
      animation: {
        "pulse-slow": "pulse 4s ease-in-out infinite"
      }
    }
  },
  plugins: []
};

export default config;
